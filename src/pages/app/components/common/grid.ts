import { type CSSProperties } from "vue"

export const GRID_CELL_STYLE: CSSProperties = { backgroundColor: 'var(--el-card-bg-color)' }
export const GRID_WRAPPER_STYLE: CSSProperties = { backgroundColor: 'var(--el-border-color)' }